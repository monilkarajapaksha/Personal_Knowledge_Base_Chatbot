langchain==0.3.7
langchain-community==0.3.7
langchain-chroma==0.1.4
langchain-huggingface==0.1.2
langchain-text-splitters==0.3.2
chromadb==0.4.24
sentence-transformers==3.2.1
groq==0.11.0
protobuf>=4.25.0

# Notes:
# - If you hit version conflicts in Colab, run `!pip install -U protobuf` and
#   restart the runtime before re-running installs.
# - These versions were current as of mid-2026; if a package has moved on,
#   loosen the pin (e.g. `langchain>=0.3.7`) and re-test.
